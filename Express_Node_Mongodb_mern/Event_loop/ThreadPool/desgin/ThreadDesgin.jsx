┌────────────────────────────────────────────────────────────┐
│                    🧠 Node.js Architecture                 │
└────────────────────────────────────────────────────────────┘

             ┌───────────────────────────────┐
             │         Main Thread           │
             │ (Event Loop + JS Execution)   │
             └───────────────────────────────┘
                           │
                           ▼
┌────────────────────────────────────────────────────────────┐
│                🌀 Event Loop Phases (Libuv)                │
│────────────────────────────────────────────────────────────│
│  1. Timers Phase         →  setTimeout / setInterval       │
│  2. Pending Callbacks    →  Deferred system operations     │
│  3. Idle / Prepare       →  Internal libuv use             │
│  4. Poll Phase           →  I/O callbacks (fs, net, etc.)  │
│  5. Check Phase          →  setImmediate() callbacks       │
│  6. Close Callbacks      →  Cleanup e.g. socket.on('close')│
└────────────────────────────────────────────────────────────┘
                           │
          ┌────────────────┴────────────────┐
          ▼                                 ▼
 ┌─────────────────────┐           ┌─────────────────────────┐
 │ Microtasks Queue    │           │  Macrotasks Queue       │
 │ (Promises, nextTick)│           │  (Timers, I/O, etc.)    │
 └─────────────────────┘           └─────────────────────────┘
                           │
                           ▼
┌────────────────────────────────────────────────────────────┐
│                    🧩 Libuv Thread Pool                    │
│────────────────────────────────────────────────────────────│
│ Default Threads: 4  (configurable up to 128)               │
│                                                         │
│ Handles:                                                │
│  • fs.readFile / writeFile (File I/O)                   │
│  • crypto.pbkdf2 / scrypt (Encryption)                  │
│  • zlib compression (gzip/deflate)                      │
│  • dns.lookup (Blocking DNS)                            │
│                                                         │
│ Example:                                                │
│  crypto.pbkdf2 → offloaded to thread → callback returned│
└────────────────────────────────────────────────────────────┘
                           │
                           ▼
┌────────────────────────────────────────────────────────────┐
│                 🧵 Worker Threads (User-level)              │
│────────────────────────────────────────────────────────────│
│ Introduced in Node v10.5                                   │
│ Used for: CPU-heavy JS tasks (e.g. ML, JSON parsing)        │
│                                                           │
│ Each Worker = separate JS thread with its own event loop.  │
│                                                           │
│ Example:                                                   │
│  new Worker('worker.js') → runs independently              │
└────────────────────────────────────────────────────────────┘
                           │
                           ▼
┌────────────────────────────────────────────────────────────┐
│                   🧠 OS Kernel Layer                       │
│────────────────────────────────────────────────────────────│
│ Handles actual low-level I/O using:                        │
│  • epoll (Linux)                                           │
│  • kqueue (macOS/BSD)                                      │
│  • IOCP (Windows)                                          │
│                                                            │
│ Node.js + libuv communicate with kernel syscalls here.     │
└────────────────────────────────────────────────────────────┘



🧩 Key Summary Table


| Layer                 | Description                          | Threads        |
| --------------------- | ------------------------------------ | -------------- |
| **V8 + Main Thread**  | Runs JavaScript & Event Loop         | 🧵 1           |
| **libuv Thread Pool** | Handles async I/O (fs, crypto, zlib) | 🧵 4 (default) |
| **Worker Threads**    | User-created true JS threads         | 🧵 N (custom)  |
| **OS Kernel**         | Executes system-level async I/O      | Multi-threaded |


⚡ Example Flow
const fs = require('fs');
const crypto = require('crypto');

console.log('Start');

// 1️⃣ setTimeout → Timer Phase
setTimeout(() => console.log('Timer done'), 0);

// 2️⃣ fs.readFile → Thread Pool
fs.readFile('data.txt', () => console.log('File read done'));

// 3️⃣ crypto.pbkdf2 → Thread Pool
crypto.pbkdf2('pass', 'salt', 100000, 512, 'sha512', () => console.log('Crypto done'));

console.log('End');


🧭 Flow:

Main Thread → fs & crypto → Libuv Thread Pool (offloaded)
                     ↓
           Threads finish → Callback → Event Loop → JS executes
