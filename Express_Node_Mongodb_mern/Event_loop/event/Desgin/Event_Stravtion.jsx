Here is a raw ASCII diagram that clearly shows Event Loop Starvation 👇
(Interview-ready, easy to remember)

                     NODE.JS EVENT LOOP (NORMAL)
┌────────────────────────────────────────────────────────┐
│ Call Stack                                              │
│   console.log()                                         │
└───────────────┬────────────────────────────────────────┘
                │
                ▼
        ┌─────────────────┐
        │ Microtasks Queue│  (Promise / nextTick)
        └────────┬────────┘
                 │  (empties)
                 ▼
┌────────────────────────────────────────────────────────┐
│ Timers Phase (setTimeout)                               │
├────────────────────────────────────────────────────────┤
│ Poll Phase (fs, network)                                │
├────────────────────────────────────────────────────────┤
│ Check Phase (setImmediate)                              │
└────────────────────────────────────────────────────────┘
✔ Event loop progresses normally

🔴 EVENT LOOP STARVATION (BROKEN FLOW)
                     EVENT LOOP (STARVATION)
┌────────────────────────────────────────────────────────┐
│ Call Stack                                              │
│   process.nextTick()                                    │
└───────────────┬────────────────────────────────────────┘
                │
                ▼
        ┌─────────────────┐
        │ Microtasks Queue│  ❗
        │ nextTick()      │
        │ nextTick()      │
        │ nextTick()      │
        │ nextTick()      │
        │ ... forever     │
        └────────┬────────┘
                 │
                 ▼
        ❌ Event Loop NEVER REACHES
┌────────────────────────────────────────────────────────┐
│ Timers Phase (setTimeout)   ❌ STARVED                  │
│ Poll Phase (I/O)            ❌ STARVED                  │
│ Check Phase (setImmediate)  ❌ STARVED                  │
└────────────────────────────────────────────────────────┘

🧠 STARVATION WITH PROMISES
Promise.then()
   ↓
Promise.then()
   ↓
Promise.then()
   ↓
... infinite

Microtasks Queue NEVER EMPTY
→ Event Loop NEVER ADVANCES

🔑 ONE-LINE RULE (VERY IMPORTANT)
Microtasks run BEFORE every phase
If microtasks never end → starvation

⚠️ REAL WORLD IMPACT
❌ setTimeout never fires
❌ fs callbacks delayed
❌ HTTP server becomes unresponsive
❌ CPU usage high

🏁 INTERVIEW GOLD LINE

Event loop starvation occurs when continuous microtasks prevent the event loop from entering timers, poll, and check phases.