🔹 slice(), push(), pop(), shift(), unshift()

Does slice() mutate the array?

What is returned by push()?

What is returned by pop()?

Time complexity of push() vs unshift()?

Why is shift() slower than pop()?

Which methods affect the beginning of an array?